{
  "name": "charly-resume",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run; npm run report",
    "report": "node scripts/generate-html-report.js",
    "test:report": "vitest run && npm run report"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/react": "^14.0.0",
    "jsdom": "^28.0.0",
    "vite": "^7.3.1",
    "vitest": "^0.34.3",
    "vitest-html-reporter": "^1.0.0"
  }
}
